type item_name {
  itemID: Int!
  itemName: String!
  bought: Boolean!
  category: String!
}

type Query {
  items: [item_name!]!
}

type Mutation {
  toggleBought(itemID: Int!, bought: Boolean!): item_name!
}